name: floss_magnifier
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: 3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  camera: 0.12.0+2
  intl: 0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
  generate: true
